{
  "name": "kairos-telemarketing-doacoes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Integração com a API de Telemarketing (ApiSTI) da plataforma de doações, para alimentar os painéis do KairOS.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "inspect": "node scripts/inspect.js",
    "fetch": "node scripts/fetch-client.js"
  }
}
